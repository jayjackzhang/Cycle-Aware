# Cycle-Aware (Initial Version)

This repository contains the initial implementation of Cycle-Aware for circular marker detection, base on YOLOv5.

## Contents
- Mixed training dataset and three test scenes dataset.
- A model weights in `runs/train/train_seg/weights/`.
- `train.py` for training new weights.
- `detect.py` for inference on test data.

